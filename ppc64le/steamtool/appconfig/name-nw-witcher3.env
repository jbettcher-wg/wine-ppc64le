# The Witcher 3 (GOG, run-native --name nw-witcher3).
#
# No active settings.  The FEX_SMCLAZYLINK=0 stopgap that lived here for a
# few hours on 2026-09-01 was retired the same day by its own removal
# condition: fex 5bfad107d forces the lazy-SMC trio off at bridge init
# (the lane never had the drain host the lazy regime's promise requires),
# and a loaded-box save-load run played in-world clean with
# FEX_SMCLAZYLINK=1 explicitly set against the new bridge -- 0 faults,
# 0 wedges, old banner gone.  The full story is
# ppc64le/docs/sessions/2026-09-01/w3-load-regression-bisect.md.
#
# The file stays as the name-keyed appconfig mechanism's anchor for this
# title; add real settings above this comment when one is needed.
