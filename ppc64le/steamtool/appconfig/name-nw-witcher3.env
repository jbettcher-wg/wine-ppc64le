# The Witcher 3 (GOG, run-native --name nw-witcher3).
#
# FEX_SMCLAZYLINK off is the 2026-09-01 STOPGAP for the save-load worker
# crash at witcher3.exe+f4a78b (Scaleform glyph-cache corruption): across
# every leg of that day's bisect the fault appears only with lazy block
# linking ON, is timing-sensitive (worst under heavy box load), and
# survived every wine-side lever and fix -- the working theory is
# fastppcx86's open cross-thread invalidation gap (a072a9122: invalidated
# blocks' LINKS in other threads are never scrubbed), armed by the
# completeness landings relaying out every thunk module.  Remove this line
# when the fex-side per-thread link scrub lands and a loaded-box save-load
# run stays clean without it.
FEX_SMCLAZYLINK=0
